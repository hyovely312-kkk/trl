let allEvents = [];
function applyFilters() { const keyword = document.getElementById("keyword").value.toLowerCase(); const klass = document.getElementById("classFilter").value; const minConfidence = Number(document.getElementById("minConfidence").value || 0); const maxConfidence = Number(document.getElementById("maxConfidence").value || 1); const agentType = document.getElementById("agentType").value; const filtered = allEvents.filter((event) => { const haystack = formatJson(agentType ? event[agentType] : event).toLowerCase(); return (!klass || event.final_class === klass) && (event.confidence || 0) >= minConfidence && (event.confidence || 0) <= maxConfidence && (!keyword || haystack.includes(keyword) || eventTitle(event).toLowerCase().includes(keyword)); }); document.getElementById("eventRows").innerHTML = filtered.map((event) => `<tr><td>${event.event_id}</td><td>${event.created_at}</td><td>${eventTitle(event)}</td><td><span class="status ${event.final_class}">${event.final_class}</span></td><td>${confidencePct(event.confidence)}</td><td><button data-view="${event.event_id}">View</button></td></tr>`).join("") || `<tr><td colspan="6" class="muted">No matching events.</td></tr>`; }
function renderDetail(event) { localStorage.setItem("TRL_LAST_EVENT_ID", event.event_id); document.getElementById("eventDetailTitle").textContent = `${event.event_id} · ${eventTitle(event)}`; document.getElementById("eventDetail").textContent = formatJson(event); }
async function renderEvents() { allEvents = await apiGet("/api/v1/trl/events"); applyFilters(); if (allEvents[0]) renderDetail(allEvents[0]); document.querySelector(".filters").addEventListener("input", applyFilters); document.getElementById("eventRows").addEventListener("click", (click) => { const button = click.target.closest("[data-view]"); if (!button) return; renderDetail(allEvents.find((item) => item.event_id === button.dataset.view)); }); }

document.addEventListener("DOMContentLoaded", () => renderEvents().catch((error) => { document.getElementById("eventRows").innerHTML = `<tr><td colspan="6" class="muted">${error.message}. Start the backend.</td></tr>`; }).finally(() => renderStaticEvents().catch(() => { document.getElementById("staticEventRows").innerHTML = `<tr><td colspan="6" class="muted">Raw Excel event rows are local/private artifacts and are not published in the public GitHub demo.</td></tr>`; })));
async function renderStaticEvents() { const rows = (await fetchCsv("assets/data/event_analysis_rows.csv")).slice(0, 60); document.getElementById("staticEventRows").innerHTML = rows.map((row) => `<tr><td>${row.event_id}</td><td>${row.project_id}</td><td>${renderCellStatus(row.target_label)}</td><td>${renderCellStatus(row.deployment_safe_best_by_macro_f1)}</td><td>${renderCellStatus(row.deployment_safe_best_by_accuracy)}</td><td>${renderCellStatus(row.upper_bound_with_start_trl)}</td></tr>`).join(""); }
